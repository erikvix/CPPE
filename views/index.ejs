<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM"
      crossorigin="anonymous"
    />
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
  </head>
  <body>
    <main class="center">
        <nav class="card">
            <header class="text-center">
              <h1 class="title">Consulta de Presos</h1>
            </header>
              <form action="/db" method="post">
                  <h2 class="title2">Codigo de consulta</h2>
                  <div class="box">
                      <input placeholder="Nome do presidiário " class="input" type="text" name="codigo" />
                      <button class="btn" type="submit">
                        <i class='bx bx-search lupa'></i>
                        Buscar
                      </button>
                  </div>
            </main>
        </nav>
    </main>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins&display=swap');
        :root{
            --azul:#0096E2;
            --rosa: #F09ABD;
        }
        *{
            box-sizing: border-box;
            font-family: 'poppins', sans-serif;
        }
        hr{
            height: 2px;
            border-radius: 1rem;
        }
        .card{
            width: max-content;
            height: max-content;
            padding: 3rem 2rem;
            border-radius: 1rem;
            box-shadow: 10px 10px 60px #bebebe,
               -10px -10px 60px #ffffff;
        }
        .box{
            display: flex;
            align-items: center;
            gap: 0.8rem;
        }
        .title{
            color: var(--azul);
            font-weight: 600;
            font-size: 3rem;
            margin-bottom: 2rem;
        }
        .title2{
            font-size: 1.3rem;
        }
      .center {
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        gap: 1rem;
        margin-top: 10rem;
      }
      .text-center {
        border: 1px;
      }
      .input{
        padding: 0.5rem;
        border: 2px solid #CCCCCC;
        border-radius: 1rem;
      }
      .btn{
        padding: 0.5rem 1.5rem;
        border-radius: 1rem;
        background-color: var(--azul);
        color: #fff;
        display: flex;
        align-items: center;
        gap: 0.2rem;
        text-transform: uppercase;
        font-weight: 600;
      }
      .lupa{
        font-size: 1.3rem;
      }
    </style>
  </body>
</html>
